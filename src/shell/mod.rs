//! The multi-window shell (Q-052). Pure structure lives in `layout`, `panels`, `context` and
//! `commands`; `controller` exposes it to QML.

pub mod commands;
pub mod context;
pub mod layout;
pub mod panels;
